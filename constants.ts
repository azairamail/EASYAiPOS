import { MenuItem, Table, InventoryItem, Order } from './types';

export const MOCK_MENU: MenuItem[] = [];
export const MOCK_TABLES: Table[] = [];
export const MOCK_INVENTORY: InventoryItem[] = [];
export const MOCK_ORDERS: Order[] = [];
